export interface PlanItem {
  icon: React.ReactNode;
  title: string;
  desc: string;
  buttonText: string;
  href: string;
}

export interface PlanetCareItem {
  icon: React.ReactNode;
  title: string;
  desc: string;
}
